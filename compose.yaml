services:
  postgres:
    image: postgres:16
    environment:
      - POSTGRES_DB=bookmanager
      - POSTGRES_USER=bookmanager
      - POSTGRES_PASSWORD=bookmanager
    ports:
      - '5432:5432'
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U bookmanager']
      interval: 5s
      timeout: 3s
      retries: 5
