# コーディングルール

- [Google の Kotlin スタイルガイド](https://developer.android.com/kotlin/style-guide)に準拠する
- 既存コードの命名・パッケージ構成・テスト構成に合わせる
- 不変条件やビジネスルールはコメントではなくドメインロジックで表現する
- 型と値オブジェクトを活用し、「不正な状態を作れない」設計を優先する
- 外部ライブラリ追加の前に、標準ライブラリで代替できないか確認する
- Result 型でドメイン層の処理結果を表現する
    - クライアント起因のエラー（4xx 相当）は Result で返し、呼び出し側に明示的なハンドリングを要求する
    - サーバ起因のエラー（5xx 相当）は例外で表現し、呼び出し側に個別のハンドリングを強制しない
- ドメイン層以外ではResult型を利用しない